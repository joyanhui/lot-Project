name: CI M1-2 Ops Console Ts

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: "要校验的 lot-manager-aio ref（分支 / tag / commit SHA）"
        required: false
        default: "main"

permissions:
  contents: read

concurrency:
  group: ci-m1-2-ops-console-ts-${{ github.event.inputs.source_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      SOURCE_REF: ${{ github.event.inputs.source_ref }}
    steps:
      - name: Checkout private workspace
        uses: actions/checkout@v4
        with:
          repository: joyanhui/lot-manager-aio
          ref: main
          token: ${{ secrets.TOKEN_GH }}
          path: workspace
          fetch-depth: 0

      - name: Switch private workspace to requested ref
        working-directory: workspace
        shell: bash
        run: |
          set -euo pipefail
          if ! git rev-parse --verify --quiet "${SOURCE_REF}^{commit}" >/dev/null; then
            git fetch origin "${SOURCE_REF}" --tags --force || git fetch origin --tags --force
          fi
          git checkout --detach "${SOURCE_REF}" || git checkout --detach "origin/${SOURCE_REF}"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2

      - name: Install workspace dependencies
        working-directory: workspace
        shell: bash
        run: bun install --frozen-lockfile

      - name: Typecheck shared layer
        working-directory: workspace/lot-shared-lib/TypeScript
        shell: bash
        run: bun run typecheck

      - name: Typecheck m1-2-ops-console-ts
        working-directory: workspace/m1-2-ops-console-ts
        shell: bash
        run: |
          set -euo pipefail
          bunx react-router typegen
          bun run typecheck

      - name: Lint m1-2-ops-console-ts
        working-directory: workspace/m1-2-ops-console-ts
        shell: bash
        run: bun run lint

      - name: Build m1-2-ops-console-ts
        working-directory: workspace/m1-2-ops-console-ts
        shell: bash
        run: |
          set -euo pipefail
          LOT_SKIP_CHECKER=1 bun run build
